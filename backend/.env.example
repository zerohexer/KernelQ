# KernelQ Environment Configuration Example
# Copy this file to .env and fill in your actual values

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id-from-console.developers.google.com
GOOGLE_CLIENT_SECRET=your-google-client-secret-from-console.developers.google.com
GOOGLE_CALLBACK_URL=https://kernelq.com/api/auth/google/callback

# Session Security
SESSION_SECRET=change-this-to-a-secure-random-string-in-production

# JWT Security (CRITICAL - must be unique and secret in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=change-this-to-a-secure-random-string-in-production

# Frontend URL for redirects
FRONTEND_URL=https://kernelq.com

# Environment
NODE_ENV=production

# Database (optional - defaults to local SQLite)
# DATABASE_PATH=/path/to/your/kernelq.db

# CORS Origins (for dynamic CORS configuration)
CORS_ORIGIN=https://kernelq.com

# Google OAuth Setup Instructions:
# 
# 1. Go to Google Cloud Console: https://console.cloud.google.com/
# 2. Create a new project or select existing project
# 3. Enable the Google+ API and Google People API
# 4. Go to "APIs & Services" → "Credentials"
# 5. Click "Create Credentials" → "OAuth 2.0 Client IDs"
# 6. Set Application type to "Web application"
# 7. Add Authorized JavaScript origins:
#    - https://kernelq.com (production)
#    - http://localhost:3000 (development)
# 8. Add Authorized redirect URIs:
#    - https://kernelq.com/api/auth/google/callback (production)
#    - http://localhost:3001/api/auth/google/callback (development)
# 9. Copy the Client ID and Client Secret to this file
# 10. Save as .env (not .env.example)